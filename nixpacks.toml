# Nixpacks configuration for Railway
# Override auto-detected build command (prevents running main.py)

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port ${PORT:-8000}"
